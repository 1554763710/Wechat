const fetchAT = require("./getAT");
const rq = require("request-promise-native");

async function fn(method, url, body = {}){
    if(url.indexOf("tags") !== -1 ){
        if(url.indexOf("get") !== -1){
            
        }
    }
}